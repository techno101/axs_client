# Client Memory

On 2026-08-12, Phase C client side: the homepage now reads the admin Builder config (`GET /v1/public/site-config?app=client`, contract v1.17.0, 5-minute cache). When configured, the hero uses the Builder's assets (photo grid or a looping video via `config.videoUrl`) and copy overrides (`heroIntro`, `primaryAction`); the gallery uses the Builder's ordered assets with captions and can be hidden. Without config (or on failure) the committed defaults (matchday folders) render — graceful fallback. Client lint/typecheck/contract:check, 34 tests and build pass.

On 2026-08-12, Phase A shipped: the homepage hero copy is always visible (the legacy intro timeline/parallax gating in `marketing-motion.tsx` was removed; section fades kept); a hero quick-book strip (next 5 days with green/yellow/red/grey availability dots) deep-links into `/book?date=…` (booking page validates and preselects the date); the matchday gallery now uses ALL 30 owner photos converted to webp under `public/images/matchday/{hero,gallery,sessions,pitches,team}/` (repeats allowed), shown as scattered tilted cards 3–5 per view with shadows and hover zoom, section raised. Contract advanced to **v1.16.0** with an additive `GET /v1/public/availability/summary?from=&to=` returning `[{date, available, total}]`; dots appear on the booking calendar popover, the date chips and the hero strip. Client lint/typecheck/contract:check, 34 tests and the production build pass. Docs-first skeleton (root `project/docs/` + this repo's `INDEX.md`) is live.

On 2026-08-11, the booking experience and home hero were rebuilt on a proper component system (Tailwind v4 + Radix primitives + class-variance-authority + lucide-react in `src/components/ui/*`): the wizard is now two flat underline-tab phases — "Pick sessions" (date chips + a real month calendar popover with Monday-first grid, disabled past/future-past-90-days, prev/next months; field photo cards with session tiles) and "Your details" (contact, per-session add-on selects, voucher, itemized totals). All status indicators are minimal dot+text (no capsule pills), all times display in Malaysia 12-hour AM/PM ("9 AM – 3 PM") across wizard, basket, details, result/find/account pages, fields pages and home session cards. Copy is customer-plain (no technical notes; the unavailable-payment case shows one short line; "no email" is a soft suggestion). The homepage hero dropped the background photo and the glitch multi-frame `match-cut-opening`: it now shows four small venue shots arranged on the left with subtle tilt/hover, text/CTA/chips on the right. The matchday gallery grew from 6 to 16 frames with tilted, elevated cards, image zoom and caption reveal on hover. `BootLoader` is now a real loader: it tracks the page's eagerly-loaded images plus fonts, shows the true loaded/total percentage on a progress bar, waits as long as actual loading takes (30s dead-network cap), and disappears the moment everything is loaded — no fixed fake duration, no scroll-lock past loading. All gates pass: lint, typecheck, 34 tests, production build + browser-bundle scan.

On 2026-08-10, the booking wizard was redesigned to the simpler two-phase flow used by leading facility-booking sites (Playfinder/Goals style): **Pick sessions** — one scrolling screen with the date strip on top and per-field photo cards (field imagery from the admin-provided `field.image`) showing that field's session tiles with time/price/availability; tapping a tile adds/removes it from a persistent sticky basket bar (count, running total, Continue). **Your details** — contact, per-session add-ons, voucher and itemized totals on one screen, with the existing server-authoritative payment flow unchanged (hold group → order → provider redirect). The old five-step date→field→session→details→review wizard and its right-hand summary panel are gone. The homepage hero wrong-photo issue was asset-level, not code: the committed `home-hero.webp` held a dark 1440×960 archive frame while the correct bright 3840px match photo existed only in the working tree; the corrected venue webp set is now committed, so builds/deploys render the intended bright match hero (and the `venue-overview.webp` swap that affected the PageHero default is corrected too). Client lint/typecheck/contract:check, all tests and the production build pass.

On 2026-08-10, the booking wizard joined the admin-owned products/addons/vouchers milestone: the Review step now shows per-session add-on quantity pickers (from `GET /v1/public/config` `addons`), a voucher code field that validates against the new `POST /v1/public/vouchers/validate` endpoint before payment, and itemized totals (sessions, add-ons, voucher discount, estimated total). `createOrder` sends `addons` (with `catalogItemId/fieldId/blockCode/bookingDate/quantity`) and `voucherCode`; the client contract mirrors the admin OpenAPI (v1.15.0) including `Order.addonLines/addonTotalMinor/voucher`. The server re-validates everything at order creation, so wizard estimates are indicative only. Client lint/typecheck/contract:check, all 34 unit/component tests and the production build passed (the first build attempt hit a corrupted `.next` cache and passed after clearing it).

On 2026-08-09, following the owner's feedback that the dark home theme felt wrong, the home experience and shared shell moved to a **pure white minimal matchday system** (blit.studio-inspired). After a follow-up review, the video backgrounds were removed (they caused a pulsing/shaking feel and network weight) and replaced with static imagery: the hero uses the bright 3840px match photo, the morning session card uses `session-day.webp` (from the 4000×2252 match shot) and the evening card uses `session-night.webp` (from the dusk-toned `team winning moment after match.jpg`), and the final CTA uses the action shot. All venue WebPs were regenerated brighter (+5% brightness, +4% contrast) from their original sources. The white palette is warm-balanced: paper `#f6f7f9`, ink `#0d1117`, one accessible green accent (`#3f7d1c` on white, `#a6e06a` on photo/dark chips), with soft-gray section rhythm (gallery/team/FAQ tinted) so it is not blinding. The loader→hero handoff is attribute-driven (`html[data-boot-done]` + `boot:done` event); the loader fully unmounts before the hero copy animates, body scroll is locked during boot, and there are no hydration mismatches. The header is page-scoped (`body:has(.match-home)`): white text over dark inner-page heroes, ink elsewhere, axe-safe. All local gates pass: lint, typecheck, contract, security scan, 34 tests, build/bundle scan, route smoke, Axe on 11 routes, motion smoke (normal + reduced, no hydration errors), visual captures and the 27-step real-browser walkthrough with 0 console errors. No BFF, booking, payment, provider, Admin or deployment authority changed.

On 2026-08-07/08, the owner directed a full redesign of the home page as an intense matchday experience, executed in five reviewed phases. The landing page is now one continuous cinematic scroll rather than labelled sections: a build-up boot loader (anime.js wordmark char stagger + pitch-line draw), a fixed match clock that counts 0' to 90' with scroll progress, a kick-off hero with live per-character title reveal (anime.js) over a 3840px hero photo, a continuous hero-to-aerial flow with no visible break, a full-screen takeover menu (clip-path wipe + staggered giant links), in-image count-up markers over the aerial (2 pitches · 6hr sessions · 90d window), full-bleed day/night session panels (RM600/RM800 with hover states), a pinned horizontal matchday gallery with captions, team clip-reveal with staggered audiences, an animated green map-pin pulse, an anime.js height-animated FAQ accordion, and a final CTA with background media and scale-on-scroll type. The palette is locked to off-black `#0B0E13` + white + one green accent `#53A423`; blue is removed. Motion is gated by `prefers-reduced-motion` only (no pointer-type gate). All local gates pass: lint, typecheck, contract, security scan, 34 tests, build/bundle scan, route smoke, Axe on 12 routes, motion smoke (LCP ~1.9s, CLS 0.000) and visual captures. The booking page keeps its structure with the updated dark palette and micro-interactions. No BFF, booking, payment, provider, Admin or deployment authority changed.

On 2026-08-07, following the owner's review feedback, the public site moved to a fuller, more interactive experience: the navigation now points to real pages (Pitches, About, Field notes, FAQ, Contact, Find booking) instead of home-page anchors; "The ground" became "Two pitches. One venue." with the owner's both-fields aerial shot leading the section; a six-photo matchday gallery strip was added to the home page; About gained an aerial + office visual pair and Contact gained a venue photo; "Coming soon"/"on the way" placeholders were replaced with real facility facts. The palette now uses the actual brand colours sampled from the owner's logo (blue `#2353a6`, green `#53a423`/`#2e6b22`, red `#8d1c44`). A fine-pointer-only custom cursor (dot + trailing ring) and magnetic primary CTAs add interactivity, and the motion system gained hero parallax, aerial scrub, a pinned horizontal gallery on desktop and staggered hero copy reveals — all reduced-motion safe and marketing-only. The owner's full asset library (50+ files) is organized under `assets/images/` with an inventory README; the three rejected night/AI field renders were deleted from `project/assets/field/`. All local gates pass: lint, typecheck, contract, security scan, 34 tests, build/bundle scan, route smoke, Axe on 12 routes, motion smoke and visual captures. No BFF, booking, provider, Admin or deployment authority changed.

On 2026-08-06, following the owner's direct instruction to remove the remaining night/AI imagery and fix the visual treatment, the full venue set was re-derived from the `RANDOM FIELD IMAGES FOR ARMOURX SPORTS` archive at higher quality (metadata-stripped WebP), the Match Cut opening now uses three distinct archive stills (`opening-a/b/c.webp`) instead of one repeated frame, the actual `armourx field.jpg` became the venue overview, and the owner office photo (`about-office.webp`) entered the About page. The fixture server no longer serves `night-stadium.webp`/`aerial-pitch.webp`; every rejected night/stadium/aerial/matchcut master file was physically removed from `public/images/`. The stacked CSS systems were consolidated: the retired v12 "dusk" layer and the dead v12 home block were deleted (5063 → 3689 lines), heavy navy scrims on the hero and page heroes were lightened so real photography leads, the contact-sheet overlay was softened, and two real contrast defects (FAQ intro text and the `--grass` accent token) were fixed to pass Axe on all 12 routes. Local lint, typecheck, contract, security scan, 34 tests, build/bundle scan, route smoke, Axe and visual captures all pass. No BFF, booking, provider, Admin or deployment authority changed.

On 2026-08-03, the owner replaced the fictional/AI campaign-media direction with a direct instruction to use the supplied `RANDOM FIELD IMAGES FOR ARMOURX SPORTS` archive across Home, About, Fields, Book and Field notes. Twelve metadata-stripped WebP derivatives now live under `public/images/venue/`; each public fallback has a distinct archive role, apart from the intentionally shared Match Cut opening-to-hero frame. The rejected night-player and all generated/fallback media are disconnected from public source. The supplied flattened Ethnocentric title artwork remains because it is not AI imagery. The exact Google Maps location is ArmourX Sports SDN. BHD., `1.3940655,103.6340126`; its existing customer address wording remains unchanged pending the separate legal-address gate. This is a visual/media change only: no BFF, booking, provider, Admin or deployment authority changed.

On 2026-08-02, the owner rejected the v12 public visual direction while retaining its useful bilingual, BFF, test and scrolling foundations. Local `v13` was created from `origin/v12@35453aa` after proving no local/remote v13 existed. The replacement is **The Match Cut**, documented under `docs/design/` with venue context, design/motion bibles, desktop/mobile wireframes, a bilingual eight-chapter storyboard, 28-reference provenance and 16 planned still roles. The exact key line is **Book your spot**. Supplied Exo 2 is self-hosted as the live semantic family; supplied Ethnocentric is used only in flattened static title artwork because its desktop EULA forbids webfont embedding. The revision 3 typography proof and `M01/M03/M08` v2 plus `M13` v3 anchors are independently approved, including the opening-to-hero invariant, venue continuity, anatomy, blank branding and natural community-team grouping. The owner then approved static-poster implementation: the new home has one SSR navigation/hero/CTA, a settle-safe contact-sheet opening, eight connected EN/BM chapters, real-pin location, four non-repeating roles (except documented M01/M03 continuity), and a semantic title fallback. The first implementation review found and corrected accidental M08 hero-face similarity plus a static-mode opening overlay; the final implemented-home review is `APPROVED`. The candidate implementation commit `424bbb8bf8c4546e2b591fc6e929ddb690a9e503` is pushed to `origin/v13`; `origin/main` remains untouched. Local lint/type/contract/security, 33 tests, build, Axe, normal/reduced motion, keyboard interruption and media-failure checks pass. The provider clip, full master batch, final owner production acceptance, deployment, payment gate and Checkpoint 2 remain blocked/deferred.

Historically, local `v12` established a “Dusk to floodlights” candidate around “The field is yours.” That visual direction is not accepted, but its centralized English/Bahasa Melayu homepage and shell copy, explicit `/bm` routing and metadata, and restrained marketing/native-task scrolling remain reusable technical evidence. The existing owner/local `next-env.d.ts`, `.agents/`, `.claude/` and `skills-lock.json` state remains preserved and unstaged.

The release candidate also removes customer-facing implementation language and the booking state gallery, fixes root error nesting and test warnings, replaces the oversized logo delivery with WebP/Next Image, narrows field media to relative same-origin paths, and adds a deterministic contract-shaped fixture Admin. Local lint, typecheck, 33 tests, contract/security checks, production build, route/metadata/sitemap/reflow/keyboard checks, Axe, normal/reduced motion and visual capture pass. Normal-motion lab evidence was LCP 1280 ms and CLS 0.000.

Production acceptance is `BLOCKED`: on 2026-08-02 the deployed Client still served `v11`, `/bm` returned 404, public config reported `onlinePayment.enabled=false`, and no controlled HitPay sandbox payment/webhook confirmation was performed. Admin public readiness returned HTTP 200 with database connected, but its payload marked the instance non-authoritative. Local Admin sandbox credentials are present while both local and production environment masters remain false; production credentials are absent locally and no VS Code tunnel is active. Do not promote `v12` to `main` until exact sandbox callback/return URLs, the fingerprint-bound Admin toggle, controlled payment confirmation and live Client verification all pass.

On 2026-08-03, Checkpoint 2 `v14@f8b7235` was promoted to `origin/main`. It repinned the Client to Admin v1.15.0 and added only a same-origin owner booking-reschedule request plus safe receipt/payment references. The browser never selects a payment provider, holds a provider key or makes a provider callback. Stripe and HitPay coexist only in Admin configuration and exactly one selected provider can become effective after the separate configuration, database, callback and deployment gates.

On 2026-07-30, Checkpoint 4 on `v9` updated the pinned public payment capability with the safe effective sandbox/production mode and renders a persistent sandbox no-real-payment banner. Disabled checkout still stops before any hold/order/payment mutation. No provider secret, provider call, Vercel change, or deployment was made.

On 2026-07-30, Checkpoint 3 on `v8` added optional guest email, safe active-account prefill, reference-only masked finder/PDF download, owner-only account booking history, and result-page Copy/Download plus missing-email protection. It keeps Admin authoritative and Client browser code same-origin only. Legacy guest records are never inferred into account history. Controlled local BFF route/axe/visual, unit, contract, security, lint, typecheck and build checks passed; no Vercel deployment, live email, OAuth or provider call occurred.

On 2026-07-30, Checkpoint 2 on `v7` added the Customer-only same-origin BFF and account UI. The BFF sets Client-origin HttpOnly session cookies, strips raw auth/email/handoff values from JSON, applies mutation origin/CSRF checks and excludes all customer/auth routes from analytics. Guest flows remain intact; Google and email are unavailable unless separate Admin configuration is present. No provider call or deployment occurred.

On 2026-07-30, Checkpoint 1 moved every current public API call behind same-origin `/api/axs`. Browser code no longer receives the Operations origin. The BFF is route/method allowlisted, validates same-site mutations, bounds bodies/timeouts/request IDs, forwards only required headers, rewrites Admin-owned media URLs, and authenticates private/mutating Admin hops with a server-only credential. The separate operational-events proxy remains narrow. Local fixture routes/axe, 29 tests including the grouped hold/order/payment/status fixture, security, typecheck, lint and build/bundle scanning pass; no deployment or payment enablement occurred.

On 2026-07-28, the public client replaced its recreated shield/type logo with the exact supplied transparent ArmourXSports wordmark in the header, footer and metadata. The client contract pin and all booking authority remain unchanged.

On 2026-07-25, the client copied the admin-owned API v1.12.0 pin (`cf7e1aa1d733a66d5556879b3fffaa3e3a25a2d1ebbe253144d85750b6e18f9b`). No public UI or customer authentication was added; admin security, exports/purge, delivery, and POS updater/hardware types do not grant client authority.

On 2026-07-24, the client copied the admin-owned API v1.11.0 pin (`181432b9a86d4fe2ba5d55f75c3779737f93b84726b4de9007f250a367b55a43`). No public UI or business behavior changed, and the client still has no database, provider, Google employee-access, or POS authority.

Against the guarded local API, native-Chrome route and axe smoke checks passed for the core public routes, desktop/mobile reflow, zoom, keyboard/mobile navigation and disabled-payment pre-mutation boundary. This is local-only evidence; provider callbacks, email/OAuth and deployment remain external gates.

`axs_client` is an independent Vercel public consumer of the admin-generated pinned v1.12.0 contract (`cf7e1aa1d733a66d5556879b3fffaa3e3a25a2d1ebbe253144d85750b6e18f9b`). It owns no server authority, database connection, payment secret, webhook, admin UI or POS behavior. Its same-origin operational-report server route holds a server-only source token, rate-limits and recursively redacts bounded safe browser failure reports before forwarding them to admin; no browser bundle receives the token.

The current booking wizard reads real config/field/availability data, supports a local basket of up to 20 dated slots and uses the v1.5 grouped hold → order → one hosted-payment-attempt flow when payment is enabled. It persists only the order access token under its short-lived session key for result polling. With payment disabled, it makes no hold/order/payment request.

HitPay checkout redirect is not confirmation. The result page reads authoritative order status. Sandbox setup and production payment enablement remain admin/owner gates. Owner-pending contacts/legal/policies/media remain visibly pending rather than invented.

SEO has static metadata, robots, sitemap and WebSite JSON-LD against the placeholder origin. Analytics excludes customer/result paths. `/account` is a non-indexed inactive boundary: email/password accounts need admin-owned account/session/email work; Google OAuth and Resend remain disabled without owner configuration, and guest history is never backfilled.
