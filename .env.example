# Tracked placeholder template only. Do not put a secret in a NEXT_PUBLIC_* value.
# Local development: ignored axs_client/.env.local
# Hosted client build: Vercel dashboard environment settings (public values only)

NEXT_PUBLIC_API_ORIGIN=https://api.example.invalid
